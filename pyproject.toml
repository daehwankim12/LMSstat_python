[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lmsstat"
authors = [{ name = "Daehwan Kim", email = "kdh5358@snu.ac.kr" }]
dynamic = ["version", "description"]
dependencies = [
    "pandas", "numpy", "scipy"
]

[project.urls]
Source = "https://github.com/daehwankum12/LMSstat_python"
